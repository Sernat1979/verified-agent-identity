name: sernat

on:
  workflow_dispatch:

jobs:
  verify-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Step 1 - Start verification
        run: echo "Starting agent verification..."

      - name: Step 2 - Processing identity
        run: echo "Processing..."

      - name: Step 3 - Link human to agent
        run: echo "https://agent.billions.network/link"
